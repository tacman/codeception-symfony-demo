# Codeception Test Suite Configuration
#
# Suite for unit or integration tests.

actor: UnitTester
modules:
    enabled:
        - Symfony:
              part: services
              app_path: 'src'
              environment: 'test'
        - Asserts
        - \App\Tests\Helper\Unit
